<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="icon" href="https://lh3.googleusercontent.com/d/1FXiotvsl9jqnWOO4h0q_Y1Bl3A5Q9Cbf" type="image/x-icon">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  </head>

  <body>
    <% if user_signed_in? %>
      <%= render 'shared/navbar' %>
      <div class="container pt-4 justify-content-center" id="main">
        <%= yield %>
      </div>
    <% else %>
      <div class="auth-container">
        <%= yield %>
      </div>
    <% end %>
  </body>
</html>
